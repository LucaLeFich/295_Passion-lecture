meta {
  name: deleteWriters
  type: http
  seq: 4
}

delete {
  url: http://localhost:3333/writers/11
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
