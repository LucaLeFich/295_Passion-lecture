meta {
  name: newWriters
  type: http
  seq: 2
}

post {
  url: http://localhost:3333/writers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
  "firstname": "Isa",
      "name": "Alle"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
