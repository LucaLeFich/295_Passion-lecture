meta {
  name: newCategory
  type: http
  seq: 3
}

post {
  url: http://localhost:3333/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
    { "label": "Mystery"}
  //mystery
}

settings {
  encodeUrl: true
  timeout: 0
}
