meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: http://localhost:3333/user/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
